# 模板组件审计与中文验收

更新日期：2026-08-21

## 审计范围

- 模板选择、筛选状态、打开 Builder、更换模板、取消回退与首次保存。
- Builder 中模板相关组件的可见属性面板，以及已知与未知行为的标识。
- English / 中文切换、中文术语、中文状态下的桌面布局。

## Review 1：功能状态链路

结果：通过。

| 检查项 | 实测结果 |
|---|---|
| 未选模板 | `Open in Builder` 禁用。 |
| 选择模板 | `Annual plan with trial` 被选中后，按钮变为可用。 |
| 进入 Builder | Builder 顶部显示模板身份、名称、标签和 `Change template`。 |
| 更换并取消 | 打开模板库后 Cancel，原模板身份保持不变。 |
| 首次保存 | 已选模板的 Save 先打开模板设置；未填两条 URL 时 Continue 禁用。 |
| 填写后继续 | 填写服务条款 / 隐私政策 URL 后可进入 `Paywall publishing confirmation`。 |
| 回写 | 两个 URL 会回写到 Links 属性面板；布局设置显示当前选中的模板名。 |

## Review 2：组件与证据边界

结果：通过。组件级可用性与源端证据边界分离。

- 已可检查：Hero Image、Card、Timer、Products、Text、Image、Button、Web Paywall Button、List、Carousel 及 Links 的已见字段面板。
- `Add element` 的十种组件均可新增、选中、编辑、即时预览和删除；已通过循环新增 10 项、编辑 Products 内容并删除回归验证。
- 模板筛选分组、单选 / 多选状态可操作；本地结果直接使用 31 张源端截取的真实模板视觉，不把样本数量伪装为源端全库数量。
- 31 张本地模板卡均已在源端进入 Builder 并核对核心层级。Demo 带入各自的 Card / Feature list / List item / Carousel / Stars / Products / Purchase / Toggle / Links 核心结构与已读默认文案；Black Friday Lifetime Access 与 Christmas 的倒计时数字均由图片/卡片组成，未误标为 Timer；Halloween 的 5 张评价卡和分页轮播按源端结构可直接切换；个人报告的 Stars 为模板层，不冒充 Add element 组件；正念体验的 Toggle 默认关闭。未逐项展开的源端字段仍在组件面板中标灰。
- 已修复模板卡与 Builder 预览断链：所有模板首次打开均复用模板卡的同一份源端素材；31 张模板均可切换到“可编辑结构”，且模板层级会按源端已观察到的父子深度进入本地预览，不再展示无关的通用树。
- 已收紧模板筛选证据范围：Reviews / Trial timeline / Free trial toggle 等组件筛选只返回已由源端层级证实的模板；卡面文字或视觉元素不会被误作组件证据。
- 组件插入位置、默认值、撤销及服务端校验仍在 `B-01` 留档。所有模板的核心树已验证；只有没有逐项展开的字段继续以灰色边界说明，不把它们包装成已验证结果。
- 模板覆盖范围、与已有节点的合并、撤销以及字段的服务端校验没有被编造成可用逻辑。

## Review 3：中文与布局

结果：通过桌面视口检查。

- 语言控制台位于顶部，支持 `EN` 与 `中文` 往返切换；英文切回后恢复英文基线。
- 中文词汇为人工整理的产品词：付费墙、构建器与生成器、产品套餐、远程配置、网页付费墙、恢复购买、服务条款、隐私政策、实收款项、净实收款项、元素内容、图层标签等。
- Builder 中文桌面检查：本轮视口 `1728 x 818`；三列 Builder 宽度 `1134px`，容器与右侧属性面板的 `scrollWidth === clientWidth`，页面无横向溢出，彼此无重叠。
- 中文模板选择器：宽度 `1040px`，无横向溢出；当前筛选下展示 12 张样本卡。内容高于视口时使用弹窗内部纵向滚动，不遮挡操作按钮。
- 模板标签、预览默认文案、链接面板、属性字段和首次模板保存弹窗均已补全为中文。产品名、设备型号、URL 和货币值保留原始可识别形式。

## 林凡式验收复核

这是一轮按“可操作、状态连续、术语可读、未知不伪造”标准执行的产品复核，不是人工签字。

1. 已修复：模板选中状态从选择器带入 Builder，并能从 Layout settings 与顶部模板上下文一致读取。
2. 已修复：首次模板保存中输入的两条链接会回写到 Links 面板，不再出现“确认完成但配置仍是旧值”的断链。
3. 已修复：模板库从统一紫色示意卡切换为源端真实缩略图；瑜伽模板已实测完成选中并进入 Builder。
4. 已修复：筛选结果变化、非 Text 组件新增不再被灰色边界阻断。Demo 将它们实现为本地可用状态；仅源端合并 / 覆盖 / 撤销与字段服务端校验仍以 Evidence & gaps 记录。
5. 未发现：本轮已验证链路中的无效按钮、错误返回路由、中文横向溢出或主要区块重叠。
6. 已修复：正念体验原本只能展示源端画面；现在有可编辑树、可操作 Toggle、标题和副标题即时回写、两条轮播项以及带 `$119.88/year` 的 CTA。回归已验证源端画面与可编辑结构切换、轮播分隔符清理、标题回写、关闭到开启切换；`Toggle On / Toggle Off` 的详细字段仍按灰色边界保留。
7. 已修复：List 的嵌套入口直接创建 `List item`，其内容面板以源端可见的 `Title / Caption`、自定义图标上传、Connector 字段独立编辑；Carousel 的嵌套入口直接创建空 Card。模板已有的深度节点也会按层级落入可编辑预览。

## 仍需源端补探的强关联事项

| 编号 | 事项 | 为什么不能自行补造 |
|---|---|---|
| M-03 | 模板与现有节点的覆盖、合并、撤销 | 这会改变实际付费墙内容和可恢复性。 |
| B-01 | 未逐项展开字段的校验、多语言覆盖、撤销与保存 | 这些会改变持久化结果，当前未获源端证据。 |
| T-01 | 筛选组合、结果数量与清空规则 | 已见控件，不代表已见结果集逻辑。 |

这些事项与完整的 Paywall 还原强相关，但在获得源端观察前，继续实现只能变成猜测。

## 2026-08-21 三维回归复核

1. **模板与素材完整性：通过。** 静态核对 `templates`、`sourceTreeObserved` 与 `public/real-adapty-templates`，结果为 31 张模板、31 个已观察核心树、0 个缺失树、0 个缺失素材。
2. **可编辑父子结构：通过。** Card 只提供 List / Button / Image / Text / Timer 五项子菜单；List 直接新增 `List item`；Carousel 直接新增空 Card。模板自身的 `depth` 也会在可编辑预览中恢复为父子关系；Products 子节点不会再被父容器吞掉。
3. **构建与证据边界：通过。** `npm run build` 成功，运行中的本地 Demo 返回 HTTP 200。`B-01` 只保留字段校验、多语言覆盖、Discard / Undo、未逐项读取 Card Layout 和服务端保存规则；没有把这些未知项实现成伪成功。
