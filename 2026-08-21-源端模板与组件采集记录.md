# 2026-08-21 源端模板与组件采集记录

## 证据边界

- 来源：已登录 Adapty 应用内的 Draft Builder 与 Change template 弹窗。
- 本记录只写本轮屏幕/DOM 中可见的字段和文字；没有把未点击到底的源端规则补成事实。
- Demo 的新增、编辑、删除是可用的本地还原模型；源端的插入顺序、保存、撤销和服务端校验仍按 B-01 记录为待验证项。

## Builder 结构

本次真实 Builder 的树不是扁平的 10 个节点。可见层级包含 Hero Image、多个 Text、List、Image、Card、嵌套 List、Carousel、Products、产品文本、Purchase button、Links、Footer、Header - Terms 等节点。

`Add element` 菜单可见的十项为：

1. Text
2. Image
3. Card
4. Web Paywall Button
5. Button
6. List
7. Links
8. Timer
9. Carousel
10. Products

## 已见属性面板

| 组件 | 可见字段/状态 |
|---|---|
| Text | System (SF Pro, Roboto) Regular、字号 28、对齐、#000000 / 100%、Max lines 2、Scale text、Margin、Vertical offset |
| Hero Image | Overlay / Transparent / Flat、Image file、Switch to video、PNG/JPG 上传（最大 10MB）、Use custom media ID、Image height、Rounded rectangle、Corner radius、Padding、Vertical offset |
| List | Default icon、Icon placement、图标色与透明度、Connector thickness / color、Icon size、标题字体 Nunito Extrabold 17、说明字体 Nunito Medium 15 |
| Layout settings | Custom Fonts 警告、购买流 Products as list + purchase button、背景色、Dark mode、默认字体、默认子元素边距、Spacing、Add max width、顶部关闭按钮动作与图标样式 |

## Add element 逐项默认插入

已在同一源端 Builder 中逐项点击十个入口。以下是新增后立即出现的默认节点与 Content 面板，不用卡面文案反推：

| 入口 | 源端默认插入 / Content 面板 |
|---|---|
| Text | 新增根节点 `Text`，默认值 `text`；必填；System (SF Pro, Roboto) Regular、28、黑色、2 行、Scale text。 |
| Image | 新增根节点 `Image`，无默认媒体；必填 Image file 上传区、Use custom media ID、Image height 100 / Fill。 |
| Card | 新增空根节点 `Card`；Content 只有 `Card items` 与嵌套 `Add element`。 |
| Web Paywall Button | 新增根节点；Open paywall in = External browser；Offer 默认 Default；Button text = `Pay on web`；Secondary text 为空。 |
| Button | 新增根节点；Button action = Custom；Button action ID 为空且必填；Text = `button text`。 |
| List | 新增空根节点 `List`；Content 只有 `List items` 与嵌套 `Add element`。 |
| Links | 新增根节点；Terms of service 与 Privacy policy 默认 On，卡面同时出现 Terms / Privacy / Restore / Login。 |
| Timer | 新增根节点；Format = `hh:mm:ss`，separator = Colon，Start value = `04:59:59`，Timer mode = Reset timer on every paywall view。 |
| Carousel | 新增空根节点 `Carousel`；Content 只有 `Carousel items` 与嵌套 `Add element`。 |
| Products | 新增根节点；Products grouping = No switch (all products are visible)，Selected product = Japanese Ai - 1 Year，Products 区为空并提供 Add product。 |

Card 的嵌套 Add element 已继续验证：菜单只有 `List`、`Button`、`Image`、`Text`、`Timer` 五项；依次插入后成为 Card 的一级子节点，Text 默认值仍为 `text`，Button 为 `button text`，Image/List 为空，Timer 为 `04:59:59`。List 的嵌套 Add element 直接插入 `List item`，默认字段为 `title` 与 `caption`；Carousel 的嵌套 Add element 直接插入空 `Card`。Demo 已按三种父子关系实现。

## 组件字段对照与本轮修复

以下对照只覆盖源端已见的十个 Add element 入口；“灰色边界”表示字段页签存在但字段名或默认值没有在源端保留，Demo 不再用自造数字填充。

| 组件 | 已见且已实现的字段 / 行为 | 本轮处理 |
|---|---|---|
| Text | Text Required、Max lines 2、Scale text、System (SF Pro, Roboto) Regular、28、黑色、Margin、Vertical offset | 内容与 Max lines 已受控回写；其余已见样式字段保留。 |
| Image | Image file（PNG/JPG，10MB）、Use custom media ID、Image height 100、Aspect Fill | 修正原先错误的 Cover / 220 高度；Custom media ID 开关和输入可保存于本地节点。 |
| Card | Card items、子菜单 List / Button / Image / Text / Timer | 保留已证实的 Content；Style / Layout 未留档字段改为灰色边界。 |
| Web Paywall Button | External browser、Offer Default、Button text `Pay on web`、Secondary text | 移除未取证的 Offer 选项；按钮与 Secondary text 受控回写。 |
| Button | Action Custom、必填 Action ID、Text `button text` | Action ID 与 Text 可编辑；未取证 Style / Layout 改为灰色边界。 |
| List | List items、Default icon、Icon placement、Icon color / opacity、Connector thickness / color、Icon size、Nunito Extrabold 17 标题、Nunito Medium 15 说明 | 补齐此前缺失的 List 字段；List item 可直接新增并独立编辑 Title / Caption。未取证 Layout 改为灰色边界。 |
| Links | Terms of service / Privacy policy 默认 On、Terms / Privacy / Restore / Login 可见 | 保留两条 URL 回写与四个可见链接；Style / Layout 仍按边界展示。 |
| Timer | `hh:mm:ss`、Colon、`04:59:59`、Text before / after、Reset timer on every paywall view | 修复 Start value、前后文字的本地节点回写；未取证 Style / Layout 改为灰色边界。 |
| Carousel | Carousel items、直接新增空 Card | 删除未取证的分页圆点、尺寸等虚构字段；Style / Layout 改为灰色边界。 |
| Products | No switch (all products are visible)、Japanese Ai - 1 Year、空 Products 区、Add product | 删除原先伪造的 Black Friday / Japanese ai 样例行；保留 Add product 入口并标明其选择器逻辑未取证。 |

### 模板内部图层的归类

- 复用已见面板：`Header` / `HEADER` / `Headline` / `Subhead` / `Caption` / `Title` / `Repeats` / `Subheader` 及其编号变体进入 Text；`Image` 的编号变体进入 Image；`List` 的编号变体、Feature list / Features 进入 List；`Today`、`Day 5`、`Day 7`、`Item 0-2` 进入 List item。
- 保留内容编辑、字段明确标灰：App Icon、Logo、Stars、Discount、Divider、Terms、Verify、Tag、Footer、Footer Top Part、Purchase、Button and Links、Product / Japanese Ai - 1 Year、Toggle On / Toggle Off。它们均来自真实模板树，但本轮未完整读取独立属性面板；不能把 Text、Image 或 Button 的字段嫁接给它们。

## 模板库

源端筛选器可见的分组为 Product count、Image / video、Components、Length、Background theme、Category。当前页面可见计数：1 product (31)、More than 1 product (56)、With Image or Video (77)、No media (3)、Free trial toggle (5)、Trial timeline (24)、Reviews (7)、Timer (5)。

当前视窗中逐项可读的模板标题包括：

- Unlock the World of Knowledge
- What to expect during your free trial
- Build deeper connections, one conversation at a time
- Unlock fluency for the whole family
- Get premium document management at an exclusive price
- How your free trial works（浅色与蓝色变体）
- START YOUR YOGA JOURNEY TODAY!
- Receive your personalized report
- Create your 3D avatar
- Get Unlimited Access
- Exclusive Weather Insights: Unlock Premium Forecast
- Mindfulness Unlocked
- Your Personal First Day Workout is Ready
- BE FEARLESS!
- Black Friday 主题卡（当前只见到卡片的一部分）

顶部生产力入口的原文为 `Generate a Design with AI` 和 `Copy a Design from Your Apps`。后者明确说明复用同一应用中由 Paywall Builder 创建的设计；本轮没有观察到可复制来源，因此未伪造最终复制结果。

## 真实缩略图资产

本轮已从已登录源端的 Templates 弹窗采集五段真实模板画面，并裁出 31 张源端模板缩略图，存放在 `public/real-adapty-templates/`。Demo 的模板选择器直接渲染这些源端图像；图内的图片、排版、套餐、文案和 CTA 不由 Demo 重新绘制。

已采集的可识别模板包括知识库、免费试用、文档管理、三种独立试用时间线、瑜伽、个性报告、3D 形象、无限访问、天气、正念、训练、万圣节、黑色星期五、新年、圣诞节、AI 编辑、儿童有声书、药草指南、视频访问、设计生活、机器人 Premium Access、全量 App 解锁、浅色/深色占位条目与幻想插画模板。幻想插画卡在 Builder 中的真实名称为 `Transparent template #3`，因此以该名称入库，不自造业务名称。

## 已逐项打开 Builder 的真实模板树

以下十三项不是根据缩略图推断，而是通过 `Open in Builder`、确认 `Change template` 后读取左侧层级并对照右侧设备预览所得。

| 源端模板 | 可见 Builder 层级 | 可见默认内容 / 购买流 |
|---|---|---|
| Unlock the World of Knowledge | Hero Image、Text、Text、List、Text、Image、Text、Text、Card、嵌套 List / List item、Carousel、多个 Card / Image / Text、Products、Japanese Ai - 1 Year、Text、Card、Links、Footer、Purchase button、Header - Terms | 三项知识权益、三步七日试用、三条评价、Monthly `$79.99/month`、`START YOUR FREE 7-DAY TRIAL`。 |
| What to expect during your free trial | Hero Image、Header、List、Today、Day 2、Day 3、Footer、Products、Button、Text、Links | 三日时间线；`Try 3 days free, continue for $6.67/month`；`Get Free Access`。 |
| Premium Access | Hero Image、Headline、Subhead、List、Item 0、Item 1、Item 2、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | Today / In 5 days / In 7 days；`$79.99 / 1 year`；`Try 7 days free and subscribe`。 |
| Unlock fluency for the whole family | Hero Image、Card、Card 内两个 Text、List、Footer、Products、Text、Links | Purchase flow 显示 `Products as purchase buttons`；三条家庭权益；`Start learning now`；无独立 Purchase Button 节点。 |
| Build deeper connections, one conversation at a time | Hero Image、Card、Card 内 Text / Image、Text、Products、Japanese Ai - 1 Year、Footer、Purchase、Text、Links、Links | `James with Jenny` 场景图、`$6.67/monthly`、`START YOUR JOURNEY TOGETHER`、自动续订说明，以及 Terms / Privacy / Restore 链接。 |
| Get premium document management at an exclusive price | Hero Image、Card、Card 内 3 个 Text、Button、Image、Footer、Products、Japanese Ai - 1 Year | `SPECIAL LAUNCH OFFER`、基础方案返回按钮、礼物图片与 `CLAIM OFFER`。购买流为 `Products as purchase buttons`，价格为 `$0.99 for 2 weeks, then $1.54/week`。 |
| START YOUR YOGA JOURNEY TODAY! | Hero Image、Image、Header、Feature list、Products、Japanese Ai - 1 Year、Caption、Legal、Footer | 四条瑜伽权益；`CONTINUE`；`1 year days free, then $79.99/year`；退订提示与 iTunes 自动续订法律文案。购买流显示 `Products as purchase buttons (deprecated)`。 |
| Lifetime Access（Black Friday） | Hero Image、Top Text、Black Friday Image、Image、Card、Image、Products、Purchase、Links、Footer | `Lifetime Access`、黑五图片、`EXPIRES IN: 04:59:59`、一次性支付 `$79.99`、`Continue`。购买流为 `Products as list + purchase button`；尽管画面出现倒计时数字，真实树中没有 `Timer` 组件。 |
| Make the season brighter!（Christmas） | Hero Image、Card、Card、Products、Japanese Ai - 1 Year、Footer、Card、Purchase、Links | `SPECIAL HOLIDAY DISCOUNT!`、`50% OFF`、`LIMITED TIME ONLY`、`06:23:59:59`、年付与月付价格、`CONTINUE`。购买流为 `Products as list + purchase button`；时间数字位于 Card，不是 `Timer` 组件。 |
| BE FEARLESS!（Halloween） | Hero Image、Card、Carousel、Carousel 内 5 个 Card、Footer、Products、Purchase、Links | `BE FEARLESS!`、五条评价及 5 个分页按钮、`$79.99 / 1 year`、`Special Halloween offer`、`Continue`。购买流为 `Products as list + purchase button`。 |
| Receive your personalized report | Hero Image、Header、Stars、Feature list、Footer、Products、Japanese Ai - 1 Year、Purchase Button、Links | 深色背景、五星 `Stars` 图层、四条占星权益、`$1.54 / week, cancel anytime.`、`CONTINUE`。购买流为 `Products as list + purchase button`。`Stars` 是模板层，不属于 Add element 的十个可添加组件。 |
| Create your 3D avatar | Hero Image、Text、Header、Products、Footer、Purchase Button、Caption | `✪ ONE-TIME OFFER`、`Enjoy 7 days for FREE, then $7.99/week.`、`Continue with trial`、自动续订说明。7 天免费是文本，不是 Free trial toggle；源端顶端第二按钮为 `Restore` 文本动作。 |
| Mindfulness Unlocked | Hero Image、Card（Image / Header / Caption）、Carousel（Review 1 / Review 2）、Footer、Products、Toggle、Toggle On、Toggle Off、Links | 标题 `Mindfulness Unlocked`、两条相同的 `Amazing app!` 评价、`Not sure? Get free trial` / `Cancel anytime`，默认 Toggle 为 Off，开启色 `#c2f01e`，购买流显示 `Products as purchase buttons (deprecated)`。已读取 Card 与 Carousel 的项目名、Toggle 的关闭态字段；Toggle On / Off 子层的详细字段尚未展开。 |
| How your free trial works（白色试用卡） | Hero Image、Header、Caption、List、Footer、Products、Japanese Ai - 1 Year、Purchase Button、Links | 通过 `Open in Builder` 后的覆盖确认实际读取。标题 `HOW YOUR FREE TRIAL WORKS`，三步内容属于 List 内部展示而非独立 Today / Day 节点；购买流 `Products as list + purchase button`，年付 `$79.99/year ($6.67/month)`，CTA `Continue`。 |
| BLACK FRIDAY 50% OFF | Hero Image、Black Friday Image、Discount、List、Footer Top Part、Products、Japanese Ai - 1 Year、Footer、Button and Links | 通过 `Open in Builder` 后的覆盖确认实际读取。购买流 `Products as list + purchase button`，背景 `#24262b`；模板树没有 `Timer`，卡面中的 50% OFF 属于 `Discount` 图层。 |
| BEGIN THE NEW YEAR | Hero Image、Card、Carousel、Products、Footer | 通过 `Open in Builder` 后的覆盖确认实际读取。购买流 `Products as list + purchase button`，背景 `#121314`；卡面中的 50% OFF 位于 Card，评价位于 Carousel。 |
| How your free trial works（蓝色试用卡） | Hero Image、Header、List、Today、Day 5、Day 7、Footer、Products、Japanese Ai - 1 Year、Purchase Button、Caption | 通过 `Open in Builder` 后的覆盖确认实际读取。三步文案为免费访问、Day 5 提醒、Day 7 扣款；购买流 `Products as list + purchase button`，年付 `$79.99/year ($6.67/month)`，CTA `Continue`。 |
| Get Unlimited Access | Hero Image、App Icon、Header、Feature list、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 通过 `Open in Builder` 后的覆盖确认实际读取。深蓝背景 `#191a1c`；权益为 Create Live wallpapers、Unlock Premium content、Daily content updates、Ad-free experience；购买流 `Products as list + purchase button`，年付 `$79.99/year`，CTA `Continue →`，底部包含 Terms of Use / Privacy Policy / Restore。 |
| Exclusive Weather Insights: Unlock Premium Forecast | Hero Image、Image、Header、Terms、Verify、Divider、List 1、Divider、Tag、Header 2、Caption 2、Header 3、Caption 3、List 2、Divider、Header 4、Caption 4、List 3、Divider、Links、Legal、Footer、Purchase Button | 通过 `Open in Builder` 后的覆盖确认实际读取。购买流 `Products as list + purchase button`，黑色背景 `#000000`，可见标题为 `Exclusive Weather Insights: Unlock Premium Forecasting`，含 `Recurring billing · Cancel any time`、`Verified by App Store` 与 `Ad-Free Experience`，CTA `SUBSCRIBE NOW`。Header 2-4 等字段内容未逐项展开，Demo 明确标灰。 |
| Your Personal First Day Workout is Ready | Hero Image、Logo、HEADER 1、Image 1、Title 1、Repeats 1、Caption 1、Image 2、Title 2、Repeats 2、Caption 2、Image 3、Title 3、Repeats 3、Caption 3、Image 4、Title 4、Repeats 4、Caption 4、HEADER 2、Subheader 2、Features、HEADER 3、Subheader 3、Products、Japanese Ai - 1 Year、Links、Legal、Footer、Purchase Button | 通过 `Open in Builder` 后的覆盖确认实际读取。购买流 `Products as list + purchase button`，背景 `#f1f1f1`；已确认 Logo `POWERLY`、首项 Core Plank / 30 SECONDS 及其动作说明、CTA `Continue`。其余三项动作、后续标题与权益字段未逐项展开，Demo 明确标灰。 |
| Get access to collection of audiobooks for kids | Hero Image、Headline、List、Item 0、Item 1、Item 2、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 第一次源端打开出现平台故障页，重新加载后第二次成功。权益为 Access all quizzes and exams、Offline mode、Hands-on labs with lecturers；年付 `$79.99 / 1 year`，One-time opportunity，CTA `Continue`。 |
| Get Full Access to The Guide of Medicinal Herbs | Hero Image、Headline、Subhead、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 模板名 Overlay template #2，购买流 `Products as list + purchase button`，背景 `#2b2424`；标题为 Get Full Access to The Guide of Medicinal Herbs，副标题为 The collection of over 5,000 plants，价格 `$4.99 / 1 week`，CTA `Continue`。 |
| Get Full Access（Faster video processing） | Hero Image、Headline、List、Item 0、Item 1、Item 2、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 模板名 Transparent template #1，背景为 linear-gradient。权益为 Faster Video Processing、Exclusive Effects & Styles、No Watermark；年付 `$79.99 / 1 year`，CTA `Subscribe Now`。 |
| Design your life | Hero Image、Headline、Subhead、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 模板名 Transparent template #2，背景为 linear-gradient；副标题 Join 1 million people taking control of their sleep and energy，年付 `$79.99 / 1 year`，CTA `Subscribe Now`。 |
| Unlock the full app with 50% off today | Hero Image、Headline、Subhead、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 模板名 Flat template #3，背景 `#ffffff`；设备预览显示年付 `$79.99`（`$1.54/week`）和 CTA `Subscribe Now`。 |
| Item One | Hero Image、List、Image、Text、Products、Japanese Ai - 1 Year、Footer、Purchase、Links | 模板名 X template #0light；包含三条 Item 占位项、图片占位层与说明文字。周付 `$4.99 / 1 week`，CTA `Continue`，底部还含 Login 链接。 |
| Item One（深色） | Hero Image、List、Image、Text、Products、Japanese Ai - 1 Year、Footer、Purchase、Links | 模板名 X template #0dark；背景 `#212121`。与浅色版的组件树和占位文案一致，但在源端是独立模板卡，Demo 使用源端 Builder 设备预览裁图，不复用浅色卡面。 |
| How your free trial works（蓝色 7 日变体） | Hero Image、Header、List、Today、Day 5、Day 7、Footer、Products、Japanese Ai - 1 Year、Purchase Button、Caption | 模板名 X template #2；线性渐变背景。与带健身人物的 Overlay template #3 是两张独立源端卡，蓝卡保留 Today / Day 5 / Day 7 层级。 |
| How your free trial works（Popular VIP content） | Hero Image、Headline、List、Item 0、Item 1、Item 2、Products、Japanese Ai - 1 Year、Footer、Purchase Button | 模板名 Overlay template #3；白色背景。三项为 `Day`、`In 5 days`、`In 7 days`，CTA `Start free trial`。 |
| Transparent template #3 | Hero Image、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 线性渐变背景、幻想插画主图、`$79.99 / 1 year`、`Advantageous offer`、CTA `Start`。 |
| Choose the plan that works best for you! | Hero Video、Headline、List、Item 0、Item 1、Item 2、Footer、Products、Japanese Ai - 1 Year、Purchase Button、Links | 模板名 X template #32；左侧真实图层为 Hero Video，背景 `#ffffff`。可见权益为 Effortless AI-Powered Edits、Exclusive Filters and Effects、Save Time with Auto-Enhance；年付 `$45.99`（`$4.16/month`），CTA `Start free trial`。 |
| Start training today | Hero Image、Headline、List、Item 0、Item 1、Item 2、Products、Japanese Ai - 1 Year、Footer、Purchase Button、Links | 模板名 Flat template #1，背景 `#000000`。权益为 Access to premium art styles、Unlimited artwork creation、NFT minting；年付 `$79.99 / 1 year`，CTA `Start`。 |

三十一项的左侧可见层级和设备预览均已在源端核对；Demo 按这些核心结构做成可编辑节点。未逐层打开的字段（天气与训练的部分重复内容）仍保留灰色边界，因此 UI 标识为“源端已观察核心结构；本地可编辑复现”，不是“完整节点树”。所有模板首次进入 Builder 时均展示与模板库卡片同一份源端视觉。

### 后续采集阻断记录

- 已登录源端的 Templates 弹窗可滚动到 `Get Unlimited Access`、`Exclusive Weather Insights`、`Your Personal First Day Workout is Ready`、`BE FEARLESS!` 等卡片；但当前可用的浏览器控制层没有为滚动后的卡片暴露可点击的语义节点。没有在未选中卡片、未进入 Builder 的情况下，把任何一张卡标记为“已验证结构”。
