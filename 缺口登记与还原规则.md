# 缺口登记与还原规则

## 还原规则

1. 仅还原已实测或界面可见的结构、文案、字段、入口与禁用态。
2. 对“未点到底”的功能，只展示已经证实的前置界面；不编造点击后的成功页、失败页、数据变化或服务端规则。
3. 每个未知入口在 Demo 右侧 `Evidence & gaps` 面板中有编号，可点击查看已知事实与未知边界。
4. Demo 内的本地 Draft、新增节点、归档仅用于演示可点击的已知流程，不代表 Adapty 的真实后端实现。

## 当前 Demo 范围

`Web Paywall` 与 `Remote config` 已按产品范围从 Demo 中移除，M-06、R-01 至 R-04 也不再列作当前 Demo 的缺口或入口。Builder 内的 `Web Paywall Button` 是独立组件，仍在范围内。

## 当前缺口

| 编号 | 功能 | 已知 | 未知，故未还原 |
|---|---|---|---|
| M-01 | Live 发布与回退 | Live 的 General 页在有改动时显示 `Save & publish`。从 Live 点击 Duplicate、命名、Save 后会出现 `Paywall publishing confirmation`；Accept 后副本以 Live 状态打开。Draft 工具栏额外有 Test on Device，且本次点击 Draft 的 `Save` 后没有发布确认、状态仍为 Draft | Draft 直接升 Live、审核、版本快照、回退、停用 |
| M-02 | AI 生成提交 | Chats / Examples、提示词、8 个自定义 Paywall 风格标签已见；提交实际提示词后，当前账号提示无法检测 App Store 应用，要求在 iOS SDK settings 填写 Apple App ID | 有效 Apple App ID 下的生成产物、异步状态、额度、覆盖规则 |
| M-03 | 模板应用 | 模板库筛选、模板选中、Open in Builder 可用且已实际进入 Builder；首次保存提示 Custom Fonts，Links 的 Terms of Service / Privacy Policy URL 为必填。31 张本地模板均已逐项确认源端 Builder 的核心层级，并在 Demo 中复现为可编辑核心结构；所有模板首次打开均显示与模板卡同一份源端视觉。 | 未逐项展开字段、模板覆盖/合并范围、撤销与既有节点处理 |
| M-04 | 跨应用设计迁移 | 仅复制视觉 Builder 配置；入口显示 Search 与禁用的 Copy Selected Paywall；当前账户没有其他应用设计候选 | 有来源候选时的最终复制、资产、字体、异常与回滚 |
| M-05 | SDK 与交易闭环 | 首笔交易后 Products 锁定；Test on Device 会显示二维码、禁用但可复制的 `mobile-app.adapty.io/paywall-preview` 链接（含 app_id、paywall_id、current_locale、locales、cluster 参数）及 Done；Metrics 控件/字段 | 设备上的真实购买、恢复、权益、事件归因、非零指标 |
| U-01 | 截图上传 | 支持类型、大小、推荐尺寸已见；与导入相同，当前自动化控制层拒绝文件选择器上传，故未触发 Adapty 后端 | 精确错误提示、对象存储、裁剪/替换 |
| P-01 | Product offer selection | Draft 的 Add product 弹层只有 `月度会员` 与 `Japanese ai`。选中后当前页即时出现 `Japanese ai / 3 months / No offers for this product`；点击 Save 后仍是 Draft，但本次账户试验重载后新增项没有保留。Annual 产品显示 `Black Friday` | 保存未保留的服务端原因、其余 Offer、优惠资格与交易结果 |
| T-01 | Template filtering | 已见产品数、媒体、组件、Length、Background theme、Category 分组；本次源端观察到 `1 product (31)`、`More than 1 product (56)`、`With Image or Video (77)`、`No media (3)`，并有 All / Popular / Seasonal 分类。源端可见模板包括知识库、试用、家庭学习、文档管理、瑜伽、报告、3D 形象、天气、正念、训练、万圣节、黑五等 | 所有组合筛选的后端总数、分页和清空规则 |
| G-01 | 应用切换 | 菜单含 Test、Add a new app；新增表单实测含名称、可搜索的分类、图标多文件上传（PNG/JPG 最大 2MB）、Cancel、Add a new app | 提交创建、应用目录、权限、切换刷新、异常处理 |
| B-01 | Builder 属性保存 | 源端已见 Builder 左侧真实层级、Add element 的 10 个组件与 Localization。十项入口默认插入已逐项验证；Card 子菜单、List 直接新增 `List item`、Carousel 直接新增空 Card 已验证。Text 默认 `text`；Hero Image 有 Overlay / Transparent / Flat、图片/视频、上传、Custom media ID、Tint；List item 有图标上传、Connector、Title / Caption；Save 要求 `Paywall publishing confirmation`。 | 字段校验、多语言覆盖、Discard / Undo、Card 未逐项保留的 Layout 字段与服务端保存规则 |
| MT-01 | Metrics 数据与导出 | 已见 Add filter -> State -> 搜索/Reset/Apply，Last month 日期面板、Year/Quarter/Month/Week/Day、Group by Product、Audience based、install date checkbox、下载图标、6 个零数据指标卡及 17 列表格 | 真实数据口径、归因、筛选后数据变化、非零指标、导出文件与下载行为 |

## 与 Paywall 核心链路的关联判断

判断标准：只有会直接改变付费墙的配置、用户可见内容、定价购买、发布可访问性或权益验证的事项，才列为“强关联”。“中关联”保留在完整参考中，但不阻塞单个 Paywall 的基础还原；“弱关联”仅留档，不继续用账号操作补探。

| 优先级 | 缺口 | 判断 | 后续动作 |
|---|---|---|---|
| 强关联 | B-01 Builder 属性保存 | Builder 是 No-code Paywall 的内容与布局来源；新增节点、属性默认值、保存与重载会直接改变用户看到的付费墙 | 继续实测新增组件、保存、重载与可观察的字段变化 |
| 强关联 | P-01 Product offer selection | Product / Offer 决定可售套餐与优惠，直接影响购买价格和展示 | 继续核对 Offer 选择是否保存；受当前账号仅一个 Offer 限制时如实记录 |
| 强关联 | M-05 SDK 与交易闭环 | 设备预览、购买、恢复、权益与事件，是配置实际生效的最后一段 | 已验证预览链接；继续查看 Adapty 内可访问的测试购买/事件入口。真实商店交易需设备和商店测试环境，不能虚构 |
| 强关联 | M-01 Live 发布与回退 | 直接影响线上生命周期；Live General 页存在 `Save & publish`，复制并保存会走合规确认后生成 Live 副本 | 继续保留 Draft 直升、回退、停用为边界；不编造状态机 |
| 强关联的生产力入口 | M-03 模板应用 | 模板会直接生成 Builder 内容；已完成选中、进入 Builder、保存必填项验证 | 补 Demo 的已知应用路径；合并、覆盖、撤销仍留边界 |
| 中关联 | M-02 AI 生成提交 | 能加快制作，但手工 Builder 可完整创建 Paywall；当前已定位 Apple App ID 阻断 | 不为绕过账号配置猜测生成结果；保留真实阻断提示 |
| 中关联 | M-04 跨应用设计迁移 | 只复制视觉配置，不影响当前应用内单个 Paywall 的运行 | 当前无其他应用候选，保留空态和边界，不再虚构来源数据 |
| 中关联 | MT-01 Metrics 数据与导出 | 用于评估与运营优化，不改变 Paywall 的配置、交付或用户购买 | 保留已见零数据界面，不补造交易数据或导出文件 |
| 弱关联，可忽略补探 | U-01 Screenshot upload | 截图是后台管理素材，不决定 Paywall 的价格、内容结构或交易 | 保留格式/尺寸限制，不模拟后端存储或裁剪 |
| 弱关联，可忽略补探 | T-01 Template filtering | 仅影响模板发现效率，不影响已选模板或手工 Builder 的运行 | 保留已见筛选项与初始态，不补组合计数 |
| 弱关联，可忽略补探 | G-01 应用切换与新增应用 | 账户级多应用管理，不影响当前 Test 应用中 Paywall 的核心链路 | 保留入口与表单，不创建额外应用 |

## 已复刻的可点击范围

- Paywalls 列表：名称搜索、状态筛选、分页静态态、打开条目；Placement 筛选含 `test placement1` checkbox、Cancel 与 Apply，Apply 后入口显示已选择的位置。当前账号中应用 Placement 后两条记录仍在列表中，故不臆测结果集的过滤规则。
- Paywalls 行末更多菜单：已实测 `Metrics`、`Edit`、`Duplicate`、`Archive` 四项；Demo 分别连接到已还原的指标页、General、未保存复制流程与归档确认。
- 创建 Draft：名称、Products、截图上传入口、创建前合规确认与必选勾选禁用态。
- General：Products、首次交易锁定文案、已见 Offer 示例（Black Friday / No offers for this product / springsale）、Duplicate、Archive 完整名称确认；Draft 的 Add product 两项真实候选、即时新增态和灰色的重载未保留边界；Draft 的 Test on Device 二维码与预览链接；Live 的 `Save & publish` 和复制后合规确认分支。Draft 的 `Save` 保持 Draft，不复用发布确认。
- Builder：已还原源端的层级结构、Show on device、Add element 的 10 项菜单、组件右侧专属属性面板和手机预览。Demo 中十种组件均可新增、选中、编辑内容、删除，并即时反映到预览；左侧元素树支持拖动同级节点排序，父节点会连同子节点移动；Card 的子菜单、List 直接新增 `List item`、Carousel 直接新增空 Card 均按源端实测落地，模板中的深度节点也会进入对应父级预览。Hero Image（Overlay / Transparent / Flat、Image / Video、上传、Custom media ID、Tint）、Card、Timer、Products 及 Text / Image / Button / Web Paywall Button / List / Carousel 的已见字段模型均保留；Discard、多语言覆盖、字段校验与源端未逐项验证字段仍标灰。
- 模板库：已改为 31 张直接取自源端 Templates 弹窗或 Builder 设备预览的真实缩略图，不再用 CSS 示意卡伪造视觉。31 张模板均按源端 Builder 的不同核心节点树、已读默认文案与购买流复现；首次打开均显示与模板卡同一份源端视觉，切换到可编辑结构后维持已观察到的父子层级。产品数、媒体、组件、主题与分类筛选仍为本地可用筛选层，计数不冒充源端全库计数。首次保存的字体提示与 Links 必填 URL 已还原；合并、撤销与未逐项读取字段仍保留灰色边界。
- AI：已见 Chats / Examples、8 个风格标签、提示词、Browse examples；实际提交的当前账号失败提示为缺少 Apple App ID。有效 ID 下生成结果仍保留灰色边界。
- Design migration：已见 Search、禁用的 Copy Selected Paywall 与视觉配置限定；当前账号没有其他应用来源候选，最终复制仍保留灰色边界。
- 顶部应用切换：已见 Test 与 Add a new app；新增应用表单、24 个 Category、图标上传入口已还原，最终提交保持灰色边界。
- Metrics：Last month 日期面板、Week 粒度、Group by Product、Add filter -> State、Audience based、按安装日期过滤、下载入口、六张零数据卡和 17 列 Total 0 表格。

## 视觉标识

- 灰色背景的可点击入口表示：入口或前置界面已见，但其后续逻辑未完整实测；点击会打开对应的已知边界说明。
